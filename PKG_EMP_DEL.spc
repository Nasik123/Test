CREATE OR REPLACE PACKAGE PKG_EMP_DEL AS
  TYPE TYP_REC IS RECORD(
    EMPNO  NUMBER,
    ENAME  VARCHAR2(30),
    SAL    NUMBER,
    JOB    VARCHAR2(30),
    DEPTNO NUMBER);

  TYPE TYP_NEST IS TABLE OF TYP_REC;
  TYP_PKG TYP_NEST := TYP_NEST();
END;
/
